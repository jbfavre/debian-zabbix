[DEFAULT]
debian-branch = master30
debian-tag = debian/%(version)s
upstream-branch = upstream30
upstream-tag = upstream/%(version)s
pristine-tar = False
builder=dpkg-buildpackage -i\.git -I.git

[git-import-orig]
filter = .gitignore
merge = True
